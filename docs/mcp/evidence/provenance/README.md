# Provenance, BP topology, text and quarantine evidence

Regenerated against the rebuilt Linux desktop using authenticated loopback MCP.
Reproduce with `node scripts/mcp/desktop-demo.mjs` after building.

- `close-parallel.json` includes the native exported source geometry and actual
  simulator result. The nearby disconnected source mountain has zero coverage;
  only its real neighbor is measured. Overall attainment is unknown.
- `public-probes.json` includes the close-parallel result, unchanged security and
  semantic safety checks, both plain ORI and FOLD text through two ORI round
  trips, and unused-vertex quarantine refusals.
- `bp-reuse.json` and `bp-after-reuse.osf` verify public active clone, deletion of
  paired leaf 1, unrelated leaf creation reusing ID 1, export, publication and
  re-cloning. The unrelated replacement is not paired.
- `baseline-golden.json` records the three simulator golden-trace failures whose
  exact values reproduce on the untouched prior commit. No traces were blessed.

The split/merge, numeric-perturbation, missing/ambiguous-lineage and flattened
annotation cases also have focused unit regressions. No engine algorithm or
upstream quarantine issue was changed. Endpoint-angle measurements still do not
prove winding history, a continuous folding path or collision freedom.

Earlier Ginkgo and source-coverage evidence remains historical and unchanged.
These results supersede the earlier general claim that proximity can establish
source identity. Raw transcripts and redundant dumps remain uncommitted.
